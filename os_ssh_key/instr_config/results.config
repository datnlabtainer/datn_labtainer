check_ssh = client:ssh.stdout : CONTAINS : victim@172.20.0.6's password: 
create_key = client:ssh-keygen.stdout : CONTAINS : The key fingerprint is:
add_key = client:ssh-copy-id.stdout : CONTAINS : Number of key(s) added: 1 
confirm_ssh_key = client:ssh.stdout : CONTAINS : from 172.20.0.4
Data_Transfer = client:cat.stdout : CONTAINS : root:x:0:0:root:/root:/bin/bash
